#!/usr/bin/env bash
set -euo pipefail

password_file='./.vault_password_file'

if [[ -r "${password_file}" ]]; then
  cat "${password_file}"
elif [[ "${CI:-}" == 'true' ]]; then
  printf '%s\n' 'ansible-lint'
else
  printf 'Vault password file not found: %s\n' "${password_file}" >&2
  exit 1
fi
